<div class="wrapper">
  <div id="post_image"><h2><%= @post.title %></h2></div>

  <p>
    <%= markdown(@post.content) %>
  </p>

  <%= link_if_admin 'Edit |', edit_post_path(@post) %>
  <%= link_to 'Back', posts_path %>
</div>
